export default function PageHeader({ title, description }) {
  return (
    <header>
      <h1>Week 2 Assignment</h1>
      <p>Create a simple component and render my CPRG 306 information in it.</p>
    </header>
  );
}
